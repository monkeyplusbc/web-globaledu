import type { Plugin } from 'vue';
// import Components from '@app/views';

const plugin: Plugin = {
  install: () => {
    /**
     * Shared components
     * * Componentes compartidos de servidor a cliente
     */
    // app.use(Components);
    /**
     * Global components
     */
  },
};

export default plugin;
